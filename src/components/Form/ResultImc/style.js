import { StyleSheet, View} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems:"center",
  },
  marginTopText: {
    marginTop: 10,
    color:"white",
    fontSize:18
  },
  tinyLogo: {
    width: 100,
    height: 100,
  },
  textImcResponse: {
    fontSize:25,
    fontWeight:"bold",
    color:"white",
  },
});

export default styles